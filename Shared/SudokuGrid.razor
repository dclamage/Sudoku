@inherits ComponentRenderOnce

@foreach (var path in paths)
{
    <path d="@path.path" stroke-opacity="@path.opacity" fill="none" stroke-width="@path.strokeWidth" stroke="@path.color" stroke-linejoin="miter" transform="translate(@(path.strokeWidth * 0.5),@(path.strokeWidth * 0.5))"/>
}
